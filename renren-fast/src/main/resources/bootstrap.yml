spring:
  application:
    name: renren-fast #服务名称
  profiles:
    active: dev #开发环境配置
  cloud:
    nacos:
      config:
        file-extension: yaml #文件后缀名
        server-addr: localhost:8848 #Nacos地址